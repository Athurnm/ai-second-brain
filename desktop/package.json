{
  "name": "desktop",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "PATH=\"$HOME/.cargo/bin:$PATH\" WEBKIT_DISABLE_COMPOSITING_MODE=1 tauri dev",
    "build": "PATH=\"$HOME/.cargo/bin:$PATH\" tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
